#include"main.h"

/**
 * print_alphabet_x10 - function that will print the alphabet in lower case
 *10 times using only )putchar
 *
*/

void print_alphabet_x10(void)
{
	int i, c;

	for (i = 0; i <= 9; i++)
	{
		for (c = 'a'; c <= 'z'; c++)
			_putchar(ch);
		_putchar('\n');
	}
}
